# IT Solution - Next.js Website

Профессиональные решения в области СКУД, видеонаблюдения, серверной инфраструктуры и сетевой инженерии.

## Технологии

- **Next.js 15** - React фреймворк с App Router
- **TypeScript** - Типизация
- **Tailwind CSS** - Стилизация
- **Google Gemini AI** - AI консультант

## Локальная разработка

```bash
# Установка зависимостей
npm install

# Запуск dev сервера
npm run dev

# Сборка для продакшена
npm run build

# Локальный просмотр продакшен сборки
npm run start
```

Откройте [http://localhost:3000](http://localhost:3000) в браузере.

## Переменные окружения

Создайте файл `.env.local` в корне проекта:

```env
API_KEY=your_google_gemini_api_key
```

## Деплой на GitHub Pages

Сайт автоматически деплоится на GitHub Pages при пуше в ветку `main`.

### Настройка:

1. Создайте репозиторий на GitHub
2. В настройках репозитория → Settings → Secrets → Actions → New repository secret
3. Добавьте секрет `API_KEY` с вашим Gemini API ключом
4. В Settings → Pages → Source выберите "GitHub Actions"
5. Запушьте код в репозиторий

```bash
git init
git add .
git commit -m "Initial commit"
git branch -M main
git remote add origin https://github.com/your-username/your-repo.git
git push -u origin main
```

## Структура проекта

```
├── app/
│   ├── layout.tsx          # Root layout
│   ├── page.tsx            # Главная страница
│   ├── globals.css         # Глобальные стили
│   └── api/
│       └── ai-chat/
│           └── route.ts    # API endpoint для AI чата
├── components/             # React компоненты
│   ├── Navbar.tsx
│   ├── Hero.tsx
│   ├── Services.tsx
│   ├── AIConsultant.tsx
│   ├── Articles.tsx
│   ├── Contact.tsx
│   └── Footer.tsx
├── types.ts               # TypeScript типы
└── next.config.ts         # Next.js конфигурация
```

## Особенности

- 🎨 Современный технический дизайн с кастомными стилями
- 🤖 AI консультант на базе Google Gemini
- 📱 Полностью адаптивный дизайн
- ⚡ Оптимизированная производительность
- 🔒 Безопасная работа с API ключами через server-side routes
